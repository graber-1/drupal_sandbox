#ddev-generated
# If you remove the ddev-generated line above you
# are responsible for maintaining this file. DDEV will not then
# update it, for example if you add `additional_hostnames`, etc.

http:
  routers:
    lms-web-80-http:
      entrypoints:
        - http-80
      rule: HostRegexp(`^lms\.ddev\.site$`)
      
      service: "lms-web-80"
      ruleSyntax: v3
      tls: false
      # middlewares:
      #   - "lms-redirectHttps"
    lms-web-8025-http:
      entrypoints:
        - http-8025
      rule: HostRegexp(`^lms\.ddev\.site$`)
      
      service: "lms-web-8025"
      ruleSyntax: v3
      tls: false
      # middlewares:
      #   - "lms-redirectHttps"
    lms-xhgui-80-http:
      entrypoints:
        - http-8143
      rule: HostRegexp(`^lms\.ddev\.site$`)
      
      service: "lms-xhgui-80"
      ruleSyntax: v3
      tls: false
      # middlewares:
      #   - "lms-redirectHttps"
    
    
    
    lms-web-80-https:
      entrypoints:
        - http-443
      rule: HostRegexp(`^lms\.ddev\.site$`)
      
      service: "lms-web-80"
      ruleSyntax: v3
      
      tls: true
      
    lms-web-8025-https:
      entrypoints:
        - http-8026
      rule: HostRegexp(`^lms\.ddev\.site$`)
      
      service: "lms-web-8025"
      ruleSyntax: v3
      
      tls: true
      
    
    lms-xhgui-80-https:
      entrypoints:
        - http-8142
      rule: HostRegexp(`^lms\.ddev\.site$`)
      
      service: "lms-xhgui-80"
      ruleSyntax: v3
      
      tls: true
      
    

  middlewares:
    lms-redirectHttps:
      redirectScheme:
        scheme: https
        permanent: true

  services:
    lms-web-80:
      loadbalancer:
        servers:
          - url: http://ddev-lms-web:80
        
    lms-web-8025:
      loadbalancer:
        servers:
          - url: http://ddev-lms-web:8025
        
    
    
    lms-xhgui-80:
      loadbalancer:
        servers:
          - url: http://ddev-lms-xhgui:80
        
    
    

tls:
  certificates:
    - certFile: /mnt/ddev-global-cache/traefik/certs/lms.crt
      keyFile: /mnt/ddev-global-cache/traefik/certs/lms.key